type Query {
  findManyPokemon(page: Int, limit: Int): PaginatedPokemonResponse!
}

type Pokemon {
  id: ID!
  name: String!
  type: String!
  created_at: String!
  updated_at: String!
}

type Mutation {
  createOnePokemon(name: String!, type: String!): Pokemon!
  updateOnePokemon(id: ID!, name: String, type: String): Pokemon!
  deleteOnePokemon(id: ID!): Boolean!
}

type PaginatedPokemonResponse {
  totalCount: Int!
  totalPages: Int!
  currentPage: Int!
  pokemons: [Pokemon!]!
}
